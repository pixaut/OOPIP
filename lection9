метод insert вставляет новые элементы перед элементом на который указывает итератор, передав третий элемент count можно указать сколько раз элемент должен быть вставлен перед указанной позицией
в качестве аргументов insert может полуить 3 итератора первый - (position) указывает позицию на которую будут добавляться элементы, второй и третий - описывают добавляемый диапозон
обычно этот диапозон является частью другого контейнера

метод emplace предназначен для создания элемента непосредственно внутри вектора на указанную позицию, этот метод введён для повышения эффективности для больших классов,
за счёт исключения ненужных копий и перемещений которые возникают в методе insert (insert принимает существующий оюъект либо его копию выполняя перемещение) emplace (создаёт внутри вектора)
используем emplace когда элементыы контейнера - объекты классов и существует нужный конструктор в классе для их создания. insert может быть использован для вставки примитивных типов или когда не существует нужного конструктора

существует множество задач которые выполняются с наборами данных, а например класс вектор не содержит таких операций,в связи с этим в stl выбран атомарный подход
(создание функций не относящихся к какому либо контейнеру) для выполнения такого рода операций, в ряде случаем метод работает быстрее чем автономная функция, учитывая специфику организации элементов контейнера
рассмотрим 3 типичные ыункции библиотеки stl for_each(), random_shuffle(), sort() 
for_each() работает с любым классом контейнера, первые два аргумента - итераторы определяющие диапозон, последний - указатель на функцию
(применяет указатель на функцию ко всем элементам из диапозона), функция не должна изменять значения элементов контейнера,
её можно использовать вместо for 

random_shuffle - (заменена на shuffle) перетасовка элементов, принимает диапозон, третий параметр - генератор случайных чисел который будет использован для выполнения перестановки
в отличии от функции for_each random_shuffle требует контейнер который разрешает доступ к своим элементам в произвольном порядке
например класс вектор

Метод sort также требует random access контейнер, в перегрузке есть параметр exec которая определяет политику выполнения которая регламентирует способ вызова данного алгоритма для данного контейнера
например sequenced_policy(seq)
аргумент pred определяет способ сортирови элементов, если он не передан то используется перегрузка операции < для типа элементов который хранится в контейнере, если элементами контейнера являются пользовательские контейнеры
то обязательна перегрузка оператора < 

в отлчии от фунцкии for_each() цикл for основанный на диапозоне может изменять содержимое контейнера, при этом параметр важно указать как ссылку

обобщённое программирование
библиотека stl относится к обобщённому программированию где главное внимание уделяется алгоритмам, а в ООП основное внимание уделяется данным
общими аспектами являются абтрагирование и повторное использование кода, цель обобщённого программирование - создание кода которое не зависит от данных
для реализации обобщённого подхода необходимо использование итераторов
итератор - почти указатель, он указывает на элемент внутри контейнера, подобно тому как шаблны обеспечивают независимость алгоритмов от типа зранимых данных итераторы обеспечивают независимость от 
типа контейнера (от его внутреннего устройства), свойства которыми должен обладать итератор: нужно иметь возможность разыменования для того чтобы получить доступ к элементу на который он ссылается,
(оператор *), должна быть возможность присваивания одного итератора другому (оператор =), должна существовать возможность сравнения одного итератора с другим (== !=), должна существовать возможность перемещения итератора по элементам контейнера
(++) 
